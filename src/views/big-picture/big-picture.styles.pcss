.big-picture {
  position: fixed;
  height: 100vh;
  width: 100vw;
  left: 0;
  top: 0;
  background: rgba(0, 0, 0, 0.9);

  nav {
    padding: 0 48px;
    display: flex;
    flex-direction: row-reverse;
    color: white;
    border-bottom: 1px solid white;
    font-size: 48px;
  }
}
