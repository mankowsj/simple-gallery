@import '../../global-styles/mixins/animations.pcss';

.action-button {
  cursor: pointer;
  padding: 5px 16px;
  color: white;
  border-radius: 4px;
  transform: scale(1);
  display: inline-block;
  @include cubic(transform);
  &:hover {
    transform: scale(1.5);
  }
}
