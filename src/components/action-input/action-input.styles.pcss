.action-input {
  display: inline-block;
  vertical-align: middle;

  input {
    border-radius: 4px;
    border: 0;
    max-width: inherit;
    background: rgba(255, 255, 255, 0.3);
    padding: 6px 12px;
    font-size: inherit;
    color: white;
    height: 100%;
  }
}
